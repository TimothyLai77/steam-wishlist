# JavaScript / TypeScript Style Preferences
- Always use modern **ES6+ syntax**.
- Prefer **arrow functions** (e.g., `const myFunction = () => {}`) over traditional `function` declarations for helper functions, callbacks, and variable-assigned functions. 
- Only use traditional `function` syntax when explicitly required (such as methods needing their own `this` context, constructors, or generator functions).
- When writing React components, prefer splitting up components into separate files for maintainability, reusabiltiy, and modularity.
