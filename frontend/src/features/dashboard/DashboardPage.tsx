/**
 * Dashboard page — overview stats (game count, total wishlist value, active discounts).
 * TODO: Implement using queries from wishlistApi.
 */
const DashboardPage = () => {
  return (
    <div>DashboardPage</div>
  );
};

export default DashboardPage;
