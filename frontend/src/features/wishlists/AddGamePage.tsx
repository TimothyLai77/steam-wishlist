/**
 * Add game page — form to add a game to a wishlist by Steam AppID/URL.
 * Props: wishlistId
 * TODO: Implement using usePostGameMutation from wishlistApi.
 */
const AddGamePage = () => {
    return (
        <div>AddGamePage</div>
    );
};

export default AddGamePage;
