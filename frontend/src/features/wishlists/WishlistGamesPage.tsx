/**
 * Wishlist games page — shows games in a specific wishlist.
 * Props: wishlistId
 * TODO: Implement using useGetGamesQuery from wishlistApi.
 */
const WishlistGamesPage = () => {
    return (
        <div>WishlistGamesPage</div>
    );
};

export default WishlistGamesPage;
